function dot(x,y){
  if(u.length!=x.length)
  return 'not equal'
  else{
  s=0
for (let i = 0; i < v.length; i++) {
  x+=y[i]*x[i]
}
  if(x===0)
  return "they're ortho"
  else
  return x
  }
}
v1=[6,5,2,7,8]
v2=[3,2,9,5,2]
v3=[4,9,5,7,1]
v4=[8,5,6,4]
v5=[-3,1,1]
v6=[1,2,1]
z=[[v1,v2],[v3,v4],[v5,v6]]
for (let i = 0; i < z.length; i++) {
a=dot(z[i][0],z[i][1])
console.log(a)
}